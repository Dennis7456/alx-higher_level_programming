#!/usr/bin/node

// Prints three lines using a loop

const firstString = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
let i = 0;
for (i; i < firstString.length; i++) {
  console.log(firstString[i]);
}
